# Data

- **Shared corpora** (provided by the instructor via Camino), e.g. **FOMC transcripts**, go here.
- **Your team's dataset**: place raw files here (they're git-ignored by default — see `.gitignore`).
- Keep large/raw data out of version control; commit a small sample + a download script instead.
