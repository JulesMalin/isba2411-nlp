# Lecture 13 — version 1 (archived)

The first build of L13, kept for reference. 25 slides, 91 minutes.

Structure: Represent (tokens, vocabulary, pooling) -> transformer block (3 slides)
-> Retrieve -> Route -> Decide.

Replaced by v2 because it opened with tokens, the same place L11 opened, and
re-taught the transformer block over three slides. v2 leads with the product
working (semantic search), compresses the block to a single callback slide, and
uses the freed time for the in-class Shipping Decision game.

Everything here is self-contained: the built artifacts plus the builder scripts
that produced them, in `_source/`.
